# newAOE 游戏编程玩法 —— 封装函数与用法

本文档只写**玩法层需要调用的封装函数**（如何下令建造、研发、造兵、移动、攻击），不展开内核实现。适合写 AI 或理解“游戏里怎么操作”时查阅。

- **村民可建造的所有建筑及前提（时代、资源、前置建筑）** 见 **[村民建造操作清单.md](村民建造操作清单.md)**（该文档含内核与代码细节）。

---

## 一、玩法层怎么下指令

- **玩家**：通过点击与选面板，内部会调用 `getUsrAI()->HumanMove / HumanBuild / BuildingAction` 或 `core->addRelation(...)`。
- **AI**：在 `UsrAI::processData()` 里直接调用下面这些**封装函数**即可，无需关心内部关系表、寻路、扣资源等实现：
  - `HumanMove(SN, DR0, UR0)` — 单位移动
  - `HumanBuild(SN, BuildingNum, BlockDR, BlockUR)` — 村民建造
  - `HumanAction(SN, obSN)` — 单位对目标操作（采集/攻击/上交等）
  - `BuildingAction(SN, Action)` — 建筑行动（研发科技/造兵等）

下文按玩法场景列出**只需用到的封装函数与参数**。

---

## 二、选择村民 → 找空地 → 建造建筑

### 封装函数（玩法层只需调用）

| 场景 | 封装函数 / 调用方式 |
|------|----------------------|
| **AI 下令建造** | `HumanBuild(SN, BuildingNum, BlockDR, BlockUR)` |
| **玩家点建造按钮** | 内部执行 `core->addRelation(nowobject, BlockL, BlockU, CoreEven_CreatBuilding, true, num)`，玩法层只需选好村民和地块、点按钮。 |

**参数说明**（AI 用）：

- `SN`：村民的序列号（来自 `getInfo().farmers[i].SN`）。
- `BuildingNum`：建筑类型常量，如 `BUILDING_HOME`、`BUILDING_ARMYCAMP`、`BUILDING_STOCK`、`BUILDING_CENTER` 等（见 [config.h](config.h) 的 `BUILDING_TYPE`）。
- `BlockDR, BlockUR`：建造位置的块坐标，需为已探索、空地、陆地且平坦（内核会校验，失败返回错误码）。

**示例**：

```cpp
// 造房子
HumanBuild(myInfo.farmers[i].SN, BUILDING_HOME, x_home, y_home);

// 造军营
HumanBuild(myInfo.farmers[i].SN, BUILDING_ARMYCAMP, x_camp, y_camp);
```

空地判定、时代与资源检查、扣资源与创建地基等均由内核完成，玩法层只需传对参数；建筑列表与前提见 [村民建造操作清单.md](村民建造操作清单.md)。

### 村民所有建造行为的具体代码（仅用封装函数）

下面按建筑类型列出**仅用 `HumanBuild(村民SN, 建筑类型, BlockDR, BlockUR)`** 的写法。`blockDR`、`blockUR` 由玩法层自己选定（如从已探索空地中取），需满足已探索、陆地、平坦、无重叠；时代与资源由内核校验。

```cpp
// 假设：farmerSN = 某村民的 SN（如 getInfo().farmers[i].SN）
//       blockDR, blockUR = 选好的块坐标（0 <= blockDR < MAP_L, 0 <= blockUR < MAP_U）

// 石器时代可造
HumanBuild(farmerSN, BUILDING_HOME,       blockDR, blockUR);   // 房屋
HumanBuild(farmerSN, BUILDING_GRANARY,    blockDR, blockUR);   // 谷仓
HumanBuild(farmerSN, BUILDING_STOCK,     blockDR, blockUR);   // 仓库
HumanBuild(farmerSN, BUILDING_ARMYCAMP,  blockDR, blockUR);   // 军营
HumanBuild(farmerSN, BUILDING_DOCK,      blockDR, blockUR);   // 船坞（需靠水）

// 工具时代可造（需对应前置建筑，见村民建造操作清单）
HumanBuild(farmerSN, BUILDING_MARKET,    blockDR, blockUR);   // 市场（前置：谷仓）
HumanBuild(farmerSN, BUILDING_RANGE,     blockDR, blockUR);   // 靶场（前置：军营）
HumanBuild(farmerSN, BUILDING_STABLE,    blockDR, blockUR);   // 马厩（前置：军营）
HumanBuild(farmerSN, BUILDING_ARROWTOWER, blockDR, blockUR);  // 箭塔（前置：谷仓研发箭塔）
HumanBuild(farmerSN, BUILDING_FARM,      blockDR, blockUR);   // 农田（前置：市场）

// 铜器时代可造
HumanBuild(farmerSN, BUILDING_SIEGE,     blockDR, blockUR);   // 攻城武器厂
HumanBuild(farmerSN, BUILDING_COLLAGE,   blockDR, blockUR);   // 学院

// 铁器时代可造
HumanBuild(farmerSN, BUILDING_CENTER,    blockDR, blockUR);   // 市镇中心
```

**按时代与用途的示例（封装函数调用）**：

```cpp
tagInfo info = getInfo();
int farmerSN = info.farmers[0].SN;   // 例如用第一个空闲农民
int blockDR = 25, blockUR = 25;      // 示例坐标，实际应从空地中选取

// 开局常见：先造房再造仓/谷仓/军营
if (info.farmers.size() > 0 && info.farmers[0].NowState == HUMAN_STATE_IDLE) {
    HumanBuild(farmerSN, BUILDING_HOME,      blockDR,     blockUR);
    HumanBuild(farmerSN, BUILDING_STOCK,     blockDR + 3, blockUR);
    HumanBuild(farmerSN, BUILDING_GRANARY,   blockDR,     blockUR + 3);
    HumanBuild(farmerSN, BUILDING_ARMYCAMP, blockDR + 5, blockUR + 5);
}

// 工具时代：造市场、靶场、马厩、箭塔、农田（需已建谷仓/军营/市场等）
HumanBuild(farmerSN, BUILDING_MARKET,     blockDR + 8, blockUR);
HumanBuild(farmerSN, BUILDING_RANGE,     blockDR,     blockUR + 8);
HumanBuild(farmerSN, BUILDING_STABLE,     blockDR + 8, blockUR + 8);
HumanBuild(farmerSN, BUILDING_ARROWTOWER, blockDR + 2, blockUR + 2);
HumanBuild(farmerSN, BUILDING_FARM,       blockDR + 10, blockUR);

// 铜器时代：攻城武器厂、学院
HumanBuild(farmerSN, BUILDING_SIEGE,   blockDR + 12, blockUR);
HumanBuild(farmerSN, BUILDING_COLLAGE, blockDR,      blockUR + 12);

// 铁器时代：市镇中心
HumanBuild(farmerSN, BUILDING_CENTER, blockDR + 15, blockUR + 15);
```

以上全部为**封装函数调用**，未直接调用任何内核接口；返回值可用于 `ins_ret` 查错误码（如资源不足、未解锁、位置非法等）。

### 村民所有采集资源的具体代码（仅用封装函数）

村民采集相关只用封装函数 **`HumanAction(村民SN, 目标SN)`**：对**资源点/动物**为“前往并采集”，对**己方建筑**为“前往并上交资源或修理”。目标 SN 来自 `getInfo().resources[].SN` 或 `getInfo().buildings[].SN`。

**1. 从资源点 / 动物采集（采到手上）**

`getInfo().resources` 中每条有 `Type`、`SN`。用 `HumanAction(farmerSN, resourceSN)` 即会让该村民前往并采集。

```cpp
tagInfo info = getInfo();
int farmerSN = info.farmers[i].SN;   // 某个村民的 SN

// 按资源类型：用 HumanAction(村民SN, 该资源SN) 即可，内核会区分采集类型
for (int j = 0; j < info.resources.size(); j++) {
    int resSN = info.resources[j].SN;
    int resType = info.resources[j].Type;

    // 浆果丛（食物）
    if (resType == RESOURCE_BUSH)
        HumanAction(farmerSN, resSN);

    // 树木（木材）
    if (resType == RESOURCE_TREE)
        HumanAction(farmerSN, resSN);

    // 石矿（石头）
    if (resType == RESOURCE_STONE)
        HumanAction(farmerSN, resSN);

    // 金矿（金子）
    if (resType == RESOURCE_GOLD)
        HumanAction(farmerSN, resSN);

    // 动物（肉）：羚羊/象/狮等
    if (resType == RESOURCE_GAZELLE || resType == RESOURCE_ELEPHANT || resType == RESOURCE_LION)
        HumanAction(farmerSN, resSN);

    // 鱼（食物，需渔船）
    if (resType == RESOURCE_FISH)
        HumanAction(farmerSN, resSN);
}
```

**2. 选最近某类资源再下令（封装函数调用示例）**

```cpp
tagInfo info = getInfo();
int farmerSN = info.farmers[0].SN;
double farmerDR = info.farmers[0].DR, farmerUR = info.farmers[0].UR;

// 采浆果：找最近浆果丛
int snBerry = -1;
double disBerry = 1e9;
for (int j = 0; j < info.resources.size(); j++) {
    if (info.resources[j].Type != RESOURCE_BUSH) continue;
    double d = calDistance(farmerDR, farmerUR, info.resources[j].DR, info.resources[j].UR);
    if (d < disBerry) { disBerry = d; snBerry = info.resources[j].SN; }
}
if (snBerry >= 0) HumanAction(farmerSN, snBerry);

// 采木头：找最近树木
int snTree = -1;
double disTree = 1e9;
for (int j = 0; j < info.resources.size(); j++) {
    if (info.resources[j].Type != RESOURCE_TREE) continue;
    double d = calDistance(farmerDR, farmerUR, info.resources[j].DR, info.resources[j].UR);
    if (d < disTree) { disTree = d; snTree = info.resources[j].SN; }
}
if (snTree >= 0) HumanAction(farmerSN, snTree);

// 采石头
int snStone = -1;
double disStone = 1e9;
for (int j = 0; j < info.resources.size(); j++) {
    if (info.resources[j].Type != RESOURCE_STONE) continue;
    double d = calDistance(farmerDR, farmerUR, info.resources[j].DR, info.resources[j].UR);
    if (d < disStone) { disStone = d; snStone = info.resources[j].SN; }
}
if (snStone >= 0) HumanAction(farmerSN, snStone);

// 采金子
int snGold = -1;
double disGold = 1e9;
for (int j = 0; j < info.resources.size(); j++) {
    if (info.resources[j].Type != RESOURCE_GOLD) continue;
    double d = calDistance(farmerDR, farmerUR, info.resources[j].DR, info.resources[j].UR);
    if (d < disGold) { disGold = d; snGold = info.resources[j].SN; }
}
if (snGold >= 0) HumanAction(farmerSN, snGold);

// 狩猎动物（肉）
int snAnimal = -1;
double disAnimal = 1e9;
for (int j = 0; j < info.resources.size(); j++) {
    int t = info.resources[j].Type;
    if (t != RESOURCE_GAZELLE && t != RESOURCE_ELEPHANT && t != RESOURCE_LION) continue;
    double d = calDistance(farmerDR, farmerUR, info.resources[j].DR, info.resources[j].UR);
    if (d < disAnimal) { disAnimal = d; snAnimal = info.resources[j].SN; }
}
if (snAnimal >= 0) HumanAction(farmerSN, snAnimal);
```

**3. 上交资源到建筑（谷仓交食物、仓库交木/石/金）**

手持资源后，对**己方**谷仓/仓库/农田等使用 `HumanAction(村民SN, 建筑SN)`，内核会判定为“上交”或“修理”。

```cpp
// 假设村民已采了资源，选己方谷仓或仓库
for (int b = 0; b < info.buildings.size(); b++) {
    int buildSN = info.buildings[b].SN;
    int buildType = info.buildings[b].Type;

    // 谷仓：收食物类
    if (buildType == BUILDING_GRANARY)
        HumanAction(farmerSN, buildSN);

    // 仓库：收木材、石头、金子
    if (buildType == BUILDING_STOCK)
        HumanAction(farmerSN, buildSN);

    // 农田：可采集/上交（视内核对 BUILDING_FARM 的判定）
    if (buildType == BUILDING_FARM)
        HumanAction(farmerSN, buildSN);
}
```

**4. 资源类型常量汇总（用于按 Type 筛选）**

| 含义     | 常量名           |
|----------|------------------|
| 浆果丛   | `RESOURCE_BUSH`  |
| 树木     | `RESOURCE_TREE`  |
| 石矿     | `RESOURCE_STONE` |
| 金矿     | `RESOURCE_GOLD`  |
| 羚羊     | `RESOURCE_GAZELLE` |
| 象       | `RESOURCE_ELEPHANT` |
| 狮       | `RESOURCE_LION`  |
| 鱼       | `RESOURCE_FISH`  |

以上均为**封装函数调用** `HumanAction(farmerSN, targetSN)`，未直接调用任何内核；目标为资源则采集，为目标为己方建筑则上交/修理，具体语义由内核根据单位与目标类型处理。

---

## 三、在仓库（或市场等）内研发科技

### 封装函数（玩法层只需调用）

| 场景 | 封装函数 / 调用方式 |
|------|----------------------|
| **AI 下令研发** | `BuildingAction(建筑SN, Action)` |
| **玩家点建筑行动按钮** | 内部执行 `getUsrAI()->BuildingAction(nowobject->getglobalNum(), 行动常量)`。 |

**参数说明**：

- `建筑SN`：建筑的序列号（如 `getInfo().buildings[i].SN`）。
- `Action`：行动常量，例如：
  - 仓库：`BUILDING_STOCK_UPGRADE_USETOOL`、`BUILDING_STOCK_UPGRADE_DEFENSE_INFANTRY` 等；
  - 市场：`BUILDING_MARKET_WOOD_UPGRADE`、`BUILDING_MARKET_FARM_UPGRADE`、`BUILDING_MARKET_GOLD_UPGRADE` 等。

**示例**：

```cpp
BuildingAction(building.SN, BUILDING_STOCK_UPGRADE_USETOOL);
BuildingAction(building.SN, BUILDING_MARKET_WOOD_UPGRADE);
```

研发是否可用、资源扣除、进度推进与完成效果均由内核处理。

---

## 四、军营（或靶场、马厩等）制造士兵

### 封装函数（玩法层只需调用）

与“研发科技”相同：**BuildingAction(建筑SN, Action)**，其中 `Action` 为造兵常量即可。

| 建筑 | 造兵常量示例 |
|------|----------------|
| 军营 | `BUILDING_ARMYCAMP_CREATE_CLUBMAN`、`BUILDING_ARMYCAMP_CREATE_SLINGER`、`BUILDING_ARMYCAMP_CREATE_BROADSWORD` |
| 靶场 | `BUILDING_RANGE_CREATE_BOWMAN`、`BUILDING_RANGE_CREATE_COMPOSITE_BOWMAN` |
| 马厩 | `BUILDING_STABLE_CREATE_SCOUT`、`BUILDING_STABLE_CREATE_CAVALRY` |
| 船坞 | `BUILDING_DOCK_CREATE_SAILING`、`BUILDING_DOCK_CREATE_WOOD_BOAT`、`BUILDING_DOCK_CREATE_SHIP` |

**示例**：

```cpp
BuildingAction(building.SN, BUILDING_ARMYCAMP_CREATE_CLUBMAN);
BuildingAction(building.SN, BUILDING_RANGE_CREATE_BOWMAN);
```

单位生成位置、资源与人口检查等由内核处理。

### 所有建筑可进行的操作的具体代码（仅用封装函数）

下面按建筑类型列出**仅用 `BuildingAction(建筑SN, Action)`** 可执行的全部操作。建筑 SN 来自 `getInfo().buildings[i].SN`，且需按建筑 `Type` 选用对应 Action；是否可用、资源与时代由内核校验。

```cpp
// 假设：buildingSN = 某建筑的 SN（如 getInfo().buildings[k].SN）
//       建筑类型由 getInfo().buildings[k].Type 判定

// ---------- 市镇中心 ----------
BuildingAction(buildingSN, BUILDING_CENTER_CREATEFARMER);   // 创造村民
BuildingAction(buildingSN, BUILDING_CENTER_UPGRADE);        // 演进到工具时代

// ---------- 谷仓 ----------
BuildingAction(buildingSN, BUILDING_GRANARY_ARROWTOWER);    // 研发：建造箭塔
BuildingAction(buildingSN, BUILDING_GRANARY_WALL);          // 研发：建造城墙
BuildingAction(buildingSN, BUILDING_GRANARY_ARROWTOWE_UPGRADE);  // 箭塔相关升级

// ---------- 市场 ----------
BuildingAction(buildingSN, BUILDING_MARKET_WOOD_UPGRADE);   // 研发木材加工
BuildingAction(buildingSN, BUILDING_MARKET_STONE_UPGRADE);  // 研发石矿开采
BuildingAction(buildingSN, BUILDING_MARKET_FARM_UPGRADE);   // 研发驯养动物
BuildingAction(buildingSN, BUILDING_MARKET_GOLD_UPGRADE);  // 研发金矿开采
BuildingAction(buildingSN, BUILDING_MARKET_WHEEL_UPGRADE);  // 研发车轮
BuildingAction(buildingSN, BUILDING_MARKET_CRAFT_UPGRADE); // 研发工艺（铜器）
BuildingAction(buildingSN, BUILDING_MARKET_PLOW_UPGRADE);  // 研发犁（铜器）

// ---------- 仓库 ----------
// 以下四项均为两级科技：同一常量先研一级，研完后再调用即研二级（由内核根据当前等级处理）
BuildingAction(buildingSN, BUILDING_STOCK_UPGRADE_USETOOL);           // 一级：工具使用；二级：金属加工（铜器）
BuildingAction(buildingSN, BUILDING_STOCK_UPGRADE_DEFENSE_INFANTRY);  // 一级：步兵护甲；二级：步兵鳞甲（铜器）
BuildingAction(buildingSN, BUILDING_STOCK_UPGRADE_DEFENSE_ARCHER);    // 一级：弓兵护甲；二级：弓兵鳞甲（铜器）
BuildingAction(buildingSN, BUILDING_STOCK_UPGRADE_DEFENSE_RIDER);     // 一级：骑兵护甲；二级：骑兵鳞甲（铜器）
BuildingAction(buildingSN, BUILDING_STOCK_UPGRADE_MISSILE_DEFENSE_INFANTRY);  // 研究青铜盾（单级）

// ---------- 军营 ----------
BuildingAction(buildingSN, BUILDING_ARMYCAMP_CREATE_CLUBMAN);    // 训练棍棒兵
BuildingAction(buildingSN, BUILDING_ARMYCAMP_CREATE_SLINGER);   // 训练投石兵
BuildingAction(buildingSN, BUILDING_ARMYCAMP_UPGRADE_CLUBMAN);  // 升级为战斧
BuildingAction(buildingSN, BUILDING_ARMYCAMP_CREATE_BROADSWORD);   // 训练阔剑兵
BuildingAction(buildingSN, BUILDING_ARMYCAMP_UPGRADE_BROADSWORD);  // 升级阔剑科技
BuildingAction(buildingSN, BUILDING_ARMYCAMP_RESEARCH_LOGISTICS);  // 研究后勤：兵营单位占0.5人口

// ---------- 靶场 ----------
BuildingAction(buildingSN, BUILDING_RANGE_CREATE_BOWMAN);         // 训练弓箭手
BuildingAction(buildingSN, BUILDING_RANGE_CREATE_CHARIOT_ARCHER);  // 训练战车弓兵
BuildingAction(buildingSN, BUILDING_RANGE_CREATE_COMPOSITE_BOWMAN); // 训练复合弓兵
BuildingAction(buildingSN, BUILDING_RANGE_UPGRADE_COMPOSITE_BOW);   // 升级复合弓科技

// ---------- 马厩 ----------
BuildingAction(buildingSN, BUILDING_STABLE_CREATE_SCOUT);   // 训练侦察骑兵
BuildingAction(buildingSN, BUILDING_STABLE_CREATE_CHARIOT); // 训练四马战车
BuildingAction(buildingSN, BUILDING_STABLE_CREATE_CAVALRY); // 训练骑兵

// ---------- 船坞 ----------
BuildingAction(buildingSN, BUILDING_DOCK_CREATE_SAILING);   // 建造渔船
BuildingAction(buildingSN, BUILDING_DOCK_CREATE_WOOD_BOAT); // 建造运输船
BuildingAction(buildingSN, BUILDING_DOCK_CREATE_SHIP);      // 建造战船

// ---------- 攻城武器厂 ----------
BuildingAction(buildingSN, BUILDING_SIEGE_CREATE_STONE_THROWER);  // 建造投石车

// ---------- 学院 ----------
BuildingAction(buildingSN, BUILDING_COLLAGE_CREATE_HOPLITE);  // 训练方阵兵
```

**按建筑类型选取 SN 后调用的示例**：

```cpp
tagInfo info = getInfo();
for (size_t k = 0; k < info.buildings.size(); k++) {
    int sn = info.buildings[k].SN;
    int type = info.buildings[k].Type;

    if (type == BUILDING_CENTER) {
        if (info.Human_Num < info.Human_MaxNum)
            BuildingAction(sn, BUILDING_CENTER_CREATEFARMER);
        // BuildingAction(sn, BUILDING_CENTER_UPGRADE);  // 演进时代
    }
    if (type == BUILDING_STOCK) {
        BuildingAction(sn, BUILDING_STOCK_UPGRADE_USETOOL);
        // BuildingAction(sn, BUILDING_STOCK_UPGRADE_DEFENSE_INFANTRY); 等
    }
    if (type == BUILDING_GRANARY) {
        BuildingAction(sn, BUILDING_GRANARY_ARROWTOWER);
        // BuildingAction(sn, BUILDING_GRANARY_WALL); 等
    }
    if (type == BUILDING_MARKET) {
        BuildingAction(sn, BUILDING_MARKET_WOOD_UPGRADE);
        // BuildingAction(sn, BUILDING_MARKET_STONE_UPGRADE); 等
    }
    if (type == BUILDING_ARMYCAMP) {
        BuildingAction(sn, BUILDING_ARMYCAMP_CREATE_CLUBMAN);
        // BuildingAction(sn, BUILDING_ARMYCAMP_CREATE_SLINGER); 等
    }
    if (type == BUILDING_RANGE) {
        BuildingAction(sn, BUILDING_RANGE_CREATE_BOWMAN);
    }
    if (type == BUILDING_STABLE) {
        BuildingAction(sn, BUILDING_STABLE_CREATE_SCOUT);
    }
    if (type == BUILDING_DOCK) {
        BuildingAction(sn, BUILDING_DOCK_CREATE_SAILING);
    }
    if (type == BUILDING_SIEGE) {
        BuildingAction(sn, BUILDING_SIEGE_CREATE_STONE_THROWER);
    }
    if (type == BUILDING_COLLAGE) {
        BuildingAction(sn, BUILDING_COLLAGE_CREATE_HOPLITE);
    }
}
```

**说明**：  
- 仓库科技中，**工具使用**、**步兵护甲**、**弓兵护甲**、**骑兵护甲** 各有两级（如一级“工具使用”、二级“金属加工”）；玩法层始终用**同一个** `BuildingAction(建筑SN, BUILDING_STOCK_UPGRADE_xxx)`，研完一级后再次调用即研发二级，内核按当前等级自动选择并扣资源。  
- 房屋(BUILDING_HOME)、箭塔(BUILDING_ARROWTOWER)、农田(BUILDING_FARM) 等无上述“建筑行动”，玩法层不对其调用 `BuildingAction`。  
- 以上全部为封装函数调用，是否可执行、资源消耗与完成效果由内核处理。

---

## 五、士兵（或单位）移动

### 封装函数（玩法层只需调用）

| 场景 | 封装函数 / 调用方式 |
|------|----------------------|
| **AI 下令移动** | `HumanMove(SN, DR0, UR0)` |
| **玩家右键空地** | 内部会根据当前选中单位与点击坐标下发移动指令。 |

**参数说明**：

- `SN`：单位的序列号（如 `getInfo().farmers[i].SN` 或 `getInfo().armies[i].SN`）。
- `DR0, UR0`：目标点的**细节坐标**（double），不是块坐标。

**示例**：

```cpp
HumanMove(myInfo.armies[i].SN, mid + 100, mid - 100);
```

---

## 六、士兵（或单位）攻击敌军

### 封装函数（玩法层只需调用）

| 场景 | 封装函数 / 调用方式 |
|------|----------------------|
| **AI 下令攻击/采集/上交** | `HumanAction(SN, obSN)` |
| **玩家右键点击目标** | 内部会根据选中单位与目标下发对应指令（攻击/采集/运输等）。 |

**参数说明**：

- `SN`：执行方单位序列号（农民或军队）。
- `obSN`：目标对象序列号（来自 `getInfo().enemy_armies[i].SN`、`enemy_farmers`、`enemy_buildings`、`resources[j].SN` 等）。

**示例**：

```cpp
// 军队攻击敌方单位
HumanAction(myInfo.armies[i].SN, enemy_farmers[j].SN);

// 农民采集资源
HumanAction(myInfo.farmers[i].SN, myInfo.resources[j].SN);
```

农民对建筑可为采集/修理/上交，军队对敌方为攻击；具体语义与校验由内核根据单位类型与目标类型处理。

### 所有兵种攻击敌军的具体代码（仅用封装函数）

对敌方**单位**或**建筑**攻击统一用 **`HumanAction(己方单位SN, 目标SN)`**，目标 SN 来自 `getInfo().enemy_armies`、`enemy_farmers`、`enemy_buildings`。下面按**每个兵种**给出具体代码，一个不少。

---

**1. 棍棒兵（AT_CLUBMAN）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_CLUBMAN) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**2. 投石兵（AT_SLINGER）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_SLINGER) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**3. 弓箭手（AT_BOWMAN）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_BOWMAN) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**4. 侦察骑兵（AT_SCOUT）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_SCOUT) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**5. 战斧（AT_SWORDSMAN，升级棍棒兵）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_SWORDSMAN) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**6. AT_IMPROVED（改进型单位）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_IMPROVED) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**7. 骑兵（AT_CAVALRY）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_CAVALRY) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**8. 船（AT_SHIP）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_SHIP) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**9. 投石车（AT_STONE_THROWER）— 普攻**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_STONE_THROWER) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**10. 投石车（AT_STONE_THROWER）— 定点投射**

```cpp
tagInfo info = getInfo();
double targetDR = 400.0, targetUR = 300.0;  // 目标点细节坐标
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_STONE_THROWER) continue;
    PinPointStrike(info.armies[i].SN, targetDR, targetUR);
    break;
}
```

**定点投射的取消**：玩法层**没有**单独的“取消定点投射”封装函数。对该投石车下发**新指令**即可覆盖当前行为，从而取消正在等待或执行中的定点投射，例如：

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_STONE_THROWER) continue;
    int sn = info.armies[i].SN;
    // 方式一：下令移动，取消当前定点投射
    HumanMove(sn, info.armies[i].DR + 50.0, info.armies[i].UR);
    // 方式二：下令攻击其他目标，同样会取消定点投射
    // if (info.enemy_armies.size() > 0) HumanAction(sn, info.enemy_armies[0].SN);
    break;
}
```

玩家在界面有「取消定点投射」按钮（对应内核 ACT_STONE_THROWER_CANCEL_PINPOINT_STRIKE）；AI 只需用上述 **HumanMove** 或 **HumanAction** 达到取消效果。

**11. 祭祀（AT_PRIEST）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_PRIEST) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
// 目标不合法时内核返回 ACTION_INVALID_PRIEST_TARGET_ERROR
```

**12. 方阵兵（AT_HOPLITE）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_HOPLITE) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**13. 四马战车（AT_CHARIOT）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_CHARIOT) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**14. 战车弓箭手（AT_CHARIOT_ARCHER）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_CHARIOT_ARCHER) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**15. 阔剑兵（AT_BROADSWORDSMAN）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_BROADSWORDSMAN) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

**16. 复合弓兵（AT_COMPOSITE_BOWMAN）**

```cpp
tagInfo info = getInfo();
for (size_t i = 0; i < info.armies.size(); i++) {
    if (info.armies[i].Sort != AT_COMPOSITE_BOWMAN) continue;
    if (info.enemy_armies.size() > 0)   HumanAction(info.armies[i].SN, info.enemy_armies[0].SN);
    else if (info.enemy_farmers.size() > 0) HumanAction(info.armies[i].SN, info.enemy_farmers[0].SN);
    else if (info.enemy_buildings.size() > 0) HumanAction(info.armies[i].SN, info.enemy_buildings[0].SN);
    break;
}
```

---

**兵种常量一览（config.h 中 AT_ARMY 枚举）**

| 序号 | 兵种常量 | 含义 |
|------|----------|------|
| 1 | AT_CLUBMAN | 棍棒兵 |
| 2 | AT_SLINGER | 投石兵 |
| 3 | AT_BOWMAN | 弓箭手 |
| 4 | AT_SCOUT | 侦察骑兵 |
| 5 | AT_SWORDSMAN | 战斧 |
| 6 | AT_IMPROVED | 改进型单位 |
| 7 | AT_CAVALRY | 骑兵 |
| 8 | AT_SHIP | 船 |
| 9 | AT_STONE_THROWER | 投石车（另有 PinPointStrike 定点投射） |
| 10 | AT_PRIEST | 祭祀 |
| 11 | AT_HOPLITE | 方阵兵 |
| 12 | AT_CHARIOT | 四马战车 |
| 13 | AT_CHARIOT_ARCHER | 战车弓箭手 |
| 14 | AT_BROADSWORDSMAN | 阔剑兵 |
| 15 | AT_COMPOSITE_BOWMAN | 复合弓兵 |

以上 1～9、11～16 均为 **`HumanAction(己方单位SN, 目标SN)`**；投石车另可 **`PinPointStrike(投石车SN, DR0, UR0)`**。是否可攻击、射程与伤害由内核处理。

---

## 七、关键常量与文件索引

| 类别 | 常量/枚举 | 头文件 |
|------|-----------|--------|
| 关系事件类型 | CoreEven_JustMoveTo, CoreEven_CreatBuilding, CoreEven_Gather, CoreEven_Attacking, CoreEven_FixBuilding, CoreEven_BuildingAct, CoreEven_MissileAttack, CoreEven_Transport, CoreEven_UnLoad, CoreEven_PinPoint_Attacking | config.h |
| 指令类型 | INS_CANCEL, INS_HUMANMOVE, INS_HUMANACTION, INS_HUMANBUILD, INS_BUILDINGACTION, INS_PINPOINT_STRIKE | config.h |
| 建筑类型 | BUILDING_HOME, BUILDING_CENTER, BUILDING_STOCK, BUILDING_GRANARY, BUILDING_ARMYCAMP, BUILDING_RANGE, BUILDING_STABLE, BUILDING_MARKET, BUILDING_DOCK, BUILDING_SIEGE, BUILDING_COLLAGE 等 | config.h |
| 建筑行动（造兵/科技） | BUILDING_CENTER_CREATEFARMER, BUILDING_CENTER_UPGRADE, BUILDING_STOCK_UPGRADE_*, BUILDING_ARMYCAMP_CREATE_CLUBMAN, BUILDING_ARMYCAMP_CREATE_SLINGER, BUILDING_ARMYCAMP_RESEARCH_LOGISTICS, BUILDING_RANGE_CREATE_BOWMAN 等 | config.h |
| 兵种 | AT_CLUBMAN, AT_SLINGER, AT_BOWMAN, AT_SCOUT, AT_CAVALRY, AT_STONE_THROWER 等 | config.h |
| 单位种类 | SORT_FARMER, SORT_ARMY, SORT_BUILDING, SORT_Building_Resource, SORT_STATICRES, SORT_ANIMAL 等 | config.h |
| 农民状态 | HUMAN_STATE_IDLE, HUMAN_STATE_WALKING, HUMAN_STATE_WORKING, HUMAN_STATE_ATTACKING | config.h |
| 资源类型 | RESOURCE_BUSH, RESOURCE_TREE, RESOURCE_STONE, RESOURCE_GOLD 等 | config.h |

- **AI 可用接口与结构体**：见 [AI接口使用指南.md](AI接口使用指南.md)（getInfo、tagInfo、tagBuilding、tagFarmer、tagArmy、tagResource、ins_ret 等）。
- **指令结构**：`struct instruction`（[GlobalVariate.h](GlobalVariate.h)），含 type、SN、obSN、BlockDR、BlockUR、option、DR、UR 等。

---

## 八、流程小结（封装函数一览）

| 玩法 | 封装函数 |
|------|----------|
| 村民建造 | `HumanBuild(SN, BuildingNum, BlockDR, BlockUR)` |
| 建筑行动（研发/造兵） | `BuildingAction(建筑SN, Action)` |
| 单位移动 | `HumanMove(SN, DR0, UR0)` |
| 单位对目标操作（攻击/采集等） | `HumanAction(SN, obSN)` |
| 投石车定点投射 | `PinPointStrike(投石车SN, DR0, UR0)` |

玩法层只需调用以上五个接口；内部校验、资源、关系表与帧推进等均为内核实现，详见 [村民建造操作清单.md](村民建造操作清单.md) 等文档。
